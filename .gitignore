# YouTube Summarizer - Git Ignore File
# ======================================

# Environment and secrets
.env
*.env
!.env.example
.env.backup.*

# Configuration (contains personal channel list)
config.txt
!config.txt.example
*.lock

# AI assistant instructions
# Note: CLAUDE.md is actually tracked in git (provides guidance to Claude Code)
# .claude/ directory contains user-specific settings and should be ignored
.claude.md

# ⚠️ CRITICAL: Data and logs - NEVER REMOVE data/ FROM GITIGNORE
# The data/ directory contains videos.db which must be preserved during updates
# Keeping it gitignored ensures git operations never touch the database
data/
logs/
*.log

# Backups (created by deploy.sh)
*.tar.gz

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
env/
ENV/
.venv

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Docker
docker-compose.override.yml
*.pid

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Temporary files
*.tmp
*.bak
test_*

# Claude Code settings (contains user permissions/preferences)
.claude/
.llm_process